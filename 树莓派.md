# 树莓派

## 1.相关数据

### 1.系统配置

#### 1.树莓派IP地址

192.168.137.242

#### 2.树莓派密码

账号：jt_stars

pwd:123456789

#### 3.网络配置文件

country=CN
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
network={
    ssid="K80"
    psk="804699jc"
    priority=10
    key_mgmt=WPA-PSK#加密方式
}

#### 4.VNC密码

Taxi-Mask-Planet-Boris

#### 5.关闭Linux 桌面环境（树莓派的 Raspberry Pi OS）的 “登录密钥环（Login Keyring）” 解锁提示

（1）终端进入seahorse

（2）右键login文件夹，更改密码

（3）现在密码改为空了，后续可设置密码

## 2.相关操作

### 1.基础命令

1.gpio readall

2.minicom

安装minicom串口助手：sudo apt-get install minicom

minicom -D /dev/ttyAMA0 -b 9600   硬件串口连接上主串口，打开mini串口

3.命令行切换输入法：

fcitx-remote -s pinyin

fcitx-remote -s keyboard-us  # 切换到英语键盘
fcitx-remote -s pinyin        # 切换回拼音

4.运行项目：

cd face_recognition

python3 app.py

### 2.安装类

#### 1.安装库包命令：

sudo apt install python3-[包名]

#### 2.xrdp安装![b3438b10966cb6fd8ba233e95b7ca4a4](D:\Concerning Document\学(Learning)\Study file of college\Learning materials\专业基础课\课设\人工智能与深度学习\项目资料\推进过程的记录文件\树莓派\树莓派系统烧录\configration\b3438b10966cb6fd8ba233e95b7ca4a4.png)

#### 3.wiringpi包安装

1.下载源码 git clone https://github.com/WiringPi/WiringPi.git 

2.进入目录 cd WiringPi 

3.编译并安装 ./build 

4.验证安装是否成功 gpio -v

#### 4.minicom串口助手

sudo apt-get install minicom

#### 5.安装RGB相关库

安装neopixel库（用来控制ws2812的集成库）

sudo pip3 install rpi_ws281x adafruit-circuitpython-neopixel --break-system-packages

sudo python3 -m pip install --force-reinstall adafruit-blinka

#### 6.*安装项目依赖*

pip3 install flask opencv-python RPi.GPIO pygame --break-system-packages

#### 7.设置内网穿透：

树莓派终端：

curl -sSL https://ngrok-agent.s3.amazonaws.com/ngrok.asc \
  | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null \
  && echo "deb https://ngrok-agent.s3.amazonaws.com bookworm main" \
  | sudo tee /etc/apt/sources.list.d/ngrok.list \
  && sudo apt update \
  && sudo apt install ngrok

![0a4576db2e381129b2e16935b72d8fae](D:\Concerning Document\学(Learning)\Study file of college\Learning materials\专业基础课\课设\人工智能与深度学习\项目资料\推进过程的记录文件\树莓派\0a4576db2e381129b2e16935b72d8fae.png)

#### 8.安装语音播报相关依赖

1.安装语音合成引擎

 sudo apt install -y espeak espeak-data 

2.安装离线文字转语音

sudo apt update
sudo apt install espeak
pip3 install pyttsx3

（

虚拟环境：

sudo apt update 

sudo apt install -y python3-venv 

python3 -m venv myenv 

source myenv/bin/activate

强制安装

pip3 install pyttsx3 --break-system-packages

）

#### 9.更新人脸识别文件夹

1.rm -r face-recognition

2.ls /media/jt_stars/

3.cp -r /media/jt_stars/HP\ USB321FD/face-recognition ~/

4.





### 2.设置类

#### 1.树莓派设置配置方法

sudo raspi-config进入配置界面

#### 2.启动服务

1. ##### 创建服务文件

sudo nano /etc/systemd/system/face-gate.service

2. ##### 重新加载 systemd（让它识别新服务）

sudo systemctl daemon-reload

3. ##### 开机自启（必须执行）

sudo systemctl enable face-gate.service

4. ##### 立即启动服务

sudo systemctl start face-gate.service

5. ##### 查看状态（是否正常运行）

systemctl status face-gate.service

看到 `active (running)` 就是成功。

报错看 `journalctl -u face-gate.service`。

##### 6.常用服务命令（你会用到）

\# 重启服务（修改代码后必须执行） 

sudo systemctl restart face-gate.service

\# 停止服务 

sudo systemctl stop face-gate.service

\# 看状态 

systemctl status face-gate.service
