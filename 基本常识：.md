# 基本常识：

## 1.“Flask 应用”

通常指使用 Flask 框架开发的 Web 程序。

要理解它，可以拆分为两部分：

💧 什么是 Flask？

Flask 是一个用 Python 编写的轻量级 Web 应用框架。它被称为“微框架”，因为它只提供最核心的功能，其他功能（如数据库、表单验证）可通过扩展按需添加，非常灵活。

💻 什么是“应用”？

这里的“应用”指一个可以通过浏览器访问的 Web 服务或网站。例如：

- 博客、个人主页
- 公司官网、后台管理系统
- 提供数据接口的 RESTful API

---

🚀 一个最小的“Flask 应用”

以下代码就是一个最基础的“Flask 应用”：

python

from flask import Flask

1. 创建 Flask 应用实例

app = Flask(name)

2. 定义路由：当访问根路径 "/" 时，执行下面的函数

@app.route("/")

def hello():

return "Hello, Flask!"

3. 启动应用

if name== "main":

app.run()

运行这段代码，在浏览器访问 http://127.0.0.1:5000/，就能看到 “Hello, Flask!”。

