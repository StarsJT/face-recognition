# 项目推进安排

### 队友 A（硬件结构与可靠性）

- 舵机与门锁机构安装、供电与GPIO 隔离/限流、线束与接口定义表（哪根线哪个引脚）。
- 实测 `SERVO_*_PULSE` 与机械行程，写一页《硬件参数表》。
- 异常：断电默认关门、程序崩溃时舵机是否抖动（若需要可加看门狗或启动脚本）。

### 队友 B（嵌入式运维与演示环境）

- 树莓派开机自启（`systemd` 或 `rc.local`）、固定 IP 或热点、摄像头权限。
- `espeak`/中文语音包、音量；无头模式下的日志轮转（你们已有 `logs/app.log`）。
- 答辩当天一键启动脚本 + 备份镜像或 requirements 冻结版本。

### 队友 C（交互与“产品感”）

- `recognize.html`：状态机（检测中 / 比对中 / 成功 / 失败）、不要每 3 秒盲请求可改为“有人脸再识别”以降低误判与负载。
- 控制台：今日统计、最近日志、管理员视角（若课程允许可加只读管理员账号）。
- 拍摄引导：光线、距离、正视/侧视示意图（减少算法压力，也算工作量）。

每周固定 30 分钟站会：接口约定（例如“识别成功回调里是否自动上锁”“活体失败是否播报哪句”），避免三人各改一处 GPIO。

------

## 三、让老师觉得“任务量够、项目完整”的抓手

课设评分通常看：需求完整度、工程化、安全、可复现、分工与文档。

| 维度     | 可落地内容                                                   |
| :------- | :----------------------------------------------------------- |
| 身份识别 | 多用户 1:N 正确、阈值有数据支撑；错误时明确“陌生人”而非乱报姓名。 |
| 安全     | 活体（哪怕轻量）、连续失败锁定、日志审计、人脸文件访问控制（你们已有基础）。 |
| 硬件闭环 | 成功/失败灯语不同、电机与语音同步、自动关门或手动关门 API 演示。 |
| 工程化   | `diagnose.py` 与主程序模型路径一致、配置项集中（阈值、识别间隔、是否活体）。 |
| 文档     | 总体架构图、数据流、接口表、分工说明、测试用例表、已知限制（侧脸、强背光）。 |

答辩时建议用一条主线讲：“检测 → 活体（可选）→ 特征比对 → 策略（阈值+投票）→ 硬件联动 → 日志”，比单纯堆功能更有说服力。

------

## 四、小结

- 你们功能骨架已经够格；当前“认不出是谁”很可能不仅是阈值问题，多用户 DNN 自动识别函数里的 `else` 分支会把失败当成成功，应优先修复并回归测试。
- 三周节奏：第 1 周修 bug + 评测集，第 2 周活体/自动关门/鲁棒，第 3 周演示与文档。
- 三人分工按机械 / 运维与镜像 / 前端与体验拆开，你专注算法与核心后端，整体就会显得完整且工作量饱满。





# 工作安排：并行为主，集成为辅

### 1. 先约定“边界”（最重要）

你们已经有一条清晰边界：

- 你：只动 `app.py`（识别、阈值、活体流程、调用硬件的时机）、必要时动 `templates/`、`liveness_detection.py` 等纯软件。
- 队友：只动 `hardware_control.py`、接线说明、树莓派启动脚本、引脚常量等。

集成点就是少数几个函数，例如识别成功/失败时调用的：

- `get_hardware().on_recognition_success(username)`
- `get_hardware().on_recognition_fail(...)`
- `unlock_door` / `lock_door` / `led_on` 等

只要函数名和参数含义不变（或变更时一起改 `app.py` 一处），两边可以各改各的实现，不必天天换人。

### 2. 并行时怎么避免“合不起来”

- 用 Git：每人分支开发，`main`（或 `develop`）每周至少合并 1～2 次；合并时只解决冲突，不要囤到最后一周。
- 无硬件也能测：PC 上没有 `RPi.GPIO` 时本来就是模拟模式；你改算法在笔记本上跑 Flask + 浏览器即可；队友在树莓派上测真 GPIO。
- 写 5 行“接口说明”（不必正式文档）：例如“成功时必须先绿灯再解锁”“失败时红灯闪几次”，贴在群公告或 `RASPBERRY_PI_README.md`，避免理解不一致。

### 3. 什么时候需要“交替”或一起在场

- 改接口时：例如从 `play_voice(str)` 改成播放 mp3 文件、或增加“蜂鸣器/继电器”新参数——这时需要你改调用处 + 队友改实现，适合短会或结对 1～2 小时，而不是整周交替。
- 联调阶段：算法稳定后，上树莓派做端到端（摄像头 → 识别 → 灯/电机/语音）通常需要 1～2 次集中联调（每人带自己最新代码），发现问题再各自回去改。

### 4. 简单时间表（举例）

| 阶段       | 做法                                                         |
| :--------- | :----------------------------------------------------------- |
| 第 1～2 周 | 并行：你优化识别；队友优化 GPIO/舵机/语音/自启。每周合并一次。 |
| 第 2 周末  | 短联调：树莓派拉最新代码，跑一遍演示流程。                   |
| 第 3 周    | 仍以并行为主；答辩前再联调一次，冻结版本。                   |

结论：不需要三个人轮流“同一时段只干一块”；平时并行 + 接口不变 + 定期合并 + 关键节点短联调，集成成本最低。若你愿意，我可以根据你们仓库里实际调用点，列一张“`app.py` ↔ `hardware_control.py` 调用清单”方便队友对照（不改代码也可以做）。









# AI帮助集成的可行性分析

可以，让后面用 AI 帮你们把两边改动“合进一两个文件”是可行的，但要想省事、少翻车，建议你们并行时顺手做几件小事，否则拖到最后一刻，AI 也只能在冲突成堆、逻辑不明的情况下猜，风险会变大。

## 能行的前提

1. 边界清楚
   你主要改识别相关；队友主要改 `hardware_control.py`（或同类模块）。最后集成多半是：把队友的类/函数合并进仓库 + 在 `app.py` 里对齐调用。
2. 两边都进 Git
   各自分支开发，最后有一个共同基准（例如 `main` 上某次提交），AI 才能做有意义的 diff 和合并，而不是“发两段微信里的代码”。
3. 留一点“给人看的说明”（几十字就够）
   例如：新增了 `hw.beep()`、改了 `on_recognition_success` 里执行顺序、引脚从 17 改成别的。写进 commit message 或 README 一角。AI 合并时最吃这个。

## 需要注意的风险

- 拖到答辩前才合并：容易出现 GPIO 初始化顺序、线程、语音命令和识别回调打架，调试时间不够。
- 两边同时大改 `app.py`：冲突多，AI 也要花很多时间对齐，不如约定“硬件相关尽量只在 `hardware_control.py`”。

## 更稳妥的节奏（仍算“先并行”）

- 并行开发 80% 时间没问题。
- 中间抽 半天：把两边代码合并跑一次（树莓派或模拟模式），有问题再各自改。
- 最后再让 AI 做一次 “收尾合并 + 检查调用是否一致”，会又快又稳。

结论：行，而且很常见；只要有 Git、接口别乱改名、别完全零沟通拖到最后一晚，后面交给 AI 做集成是合理分工。若你们约定“硬件只动 `hardware_control.py`，软件只动 `app.py` 里几处 `get_hardware()` 调用”，最后集成通常就是小范围合并，AI 也能做得很好。

